name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "::notice::Deploy workflow placeholder. Tech Lead가 VM_SSH_KEY·VM_HOST·VM_USER Secrets 등록 후 본 워크플로 본문 채워서 활성화."
          echo "현재는 수동 트리거만 가능 (Actions 탭)."
