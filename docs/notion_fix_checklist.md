# 노션 정합성 정리안 (발표 전 체크리스트)

> 코드/산출문서 검증 중 발견한 **노션 워크스페이스 불일치** 목록.
> 발표 스토리라인(요양병원 기준)과 충돌하는 구버전 흔적이 핵심.
> **이 문서는 정리안만 제공 — 노션 직접 수정은 담당자(팀)가 진행한다.**
> 작성: 2026-06-05 (전체 검증 라운드)

## 🔴 발표 스토리라인과 직접 충돌 (우선)

| 노션 페이지 | 현재 상태 | 고칠 내용 |
|---|---|---|
| **주제선정** (`ae3921da…`) | 타깃이 "콜센터 + LG 인하우스" | → **요양병원**으로 전환. 현재 시연·시드·법령·ROI 전부 요양병원 기준 |
| **타깃고객** (`88b921da…`) | "콜센터 + 대기업 인하우스" 중심, "병원은 Y2 진입" | → 1차 타깃 = **요양병원(감염예방관리료 수가 신설 수혜시설)**. 콜센터는 인접확장 로드맵으로 |
| **추진배경** (`222921da…`) | "구로 콜센터 166명" 사례 중심 | → 제2차 의료관련감염 예방관리 종합대책(2023~2027) + 요양병원 감염예방관리료 신설 타임라인으로 |

## 🟡 문서 수치 불일치

| 노션 페이지 | 현재 | 고칠 내용 |
|---|---|---|
| **요구사항정의서** (`e64921da…`) | "상태: 시작 전" | → **완료**. DB 테이블 "7개" → **13개**(실제 마이그레이션 001~003 기준) |
| **7.설계서** (`374921da…`) | "앙상블 XGB+ARIMA+GAM+SHAP", "7개 테이블" | → SHAP/GAM 표현 제거(실제 스택 = XGBoost + Granger 인과검정), 테이블 **13개**로 통일 |
| **WBS** (`48a921da…`) | 팀원 "조근범" 포함(5명) | → README canonical(4명)과 통일. 실제 팀 구성 확인 |

## 🟢 유지 (잔재 아님 — 의도된 자산)

- **Urban Immune System** 페이지 (`2714a6cc…`) = **별개 모회 캡스톤**(제1회 AI 아이디어 공모전 대상작). ThinQ Sentinel이 이를 LG DX 전자 버전으로 확장한 것.
  - **삭제 금지.** 발표 시 "검증된 모회 시스템(UIS)에서 외부 감염 선행신호를 수신한다"는 **계보 강점**으로 활용.
  - 단, 심사위원 혼동 방지를 위해 "별개 모회 프로젝트"임을 명시 분리.
- 워크스페이스 최상위 경로명 "5대기" → 팀명이므로 유지 가능(선택).

## 참고 — 코드 측에서 이미 처리된 항목 (이번 PR)
- `SHAPExplainer.tsx`(SHAP 화면 노출 위험) → **삭제 완료** (실제로는 import 0건 dead code였음)
- `UISTimeline.tsx`("UIS 외부신호" 화면 텍스트) → **삭제 완료** (dead code)
- `LiveSensorChart.tsx`(1500ppm 위험선 vs 1000 기준 혼동) → **삭제 완료** (dead code, demo는 자체 1000ppm 기준 차트 사용)
- `TierBadge` `t1~t5` ↔ `MONITOR~CRITICAL` 혼재 → AlertQueue/AlertToast/mobile 목업에 잔존(이번 시연은 /demo 가 대체). **후속 정리 대상**.
